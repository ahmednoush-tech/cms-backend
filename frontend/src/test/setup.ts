import '@testing-library/jest-dom/vitest';

/**
 * Phase 3A test setup. NOTE (per explicit instruction): these
 * tests have NOT been executed in this environment — no `npm
 * install` has been run here (no network access, consistent with
 * every backend phase's stated limitation), so `vitest run` has
 * never actually invoked this file. Written as real, runnable
 * test code for when a real Node/npm environment is available.
 */
